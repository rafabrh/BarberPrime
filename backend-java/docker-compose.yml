version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: barberprime
      POSTGRES_PASSWORD: barberprime
      POSTGRES_DB: barberprime
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
